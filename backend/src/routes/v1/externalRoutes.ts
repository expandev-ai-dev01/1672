import { Router } from 'express';

const router = Router();

// This is where you will add feature-specific external/public routes.
// Example:
// import authRoutes from '@/api/v1/external/auth/routes';
// router.use('/auth', authRoutes);

export default router;
